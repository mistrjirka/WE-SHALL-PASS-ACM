#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(void)
{
    int n = 100;
    int cp = 1;
    int left_sum = 0;
    int rght_sum = 0;
    int found = 0;
    bool finding = true;
    while (found < 10)
    {
        left_sum = (cp * (cp - 1)) / 2;
        right_sum = 

        while (finding)
        {
        }
    }
    return 0;
}