#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int n_samples = 0;
    scanf("%d", &n_samples);
    int intervals[n_samples][2];
    for (int i = 0; i < n_samples; i++)
    {
    }
    return 0;
}